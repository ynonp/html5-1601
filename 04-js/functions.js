function f1(x, y) {
  var sum = x + y;
  return sum;
}

function f2() {
  for (var i=0; i < arguments.length; i++) {
    console.log('Arg ' + i + ' = ' + arguments[i]);
  }
}
if ( ( 2 < 3 ) && ( 2 < 5 ) ) {
}

